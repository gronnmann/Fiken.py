- /companies/{company_slug}/groups
- init method for all classes that can be submitted
- maybe remove _getFromURL and use the other method?
- when checking POST classes, check if is_new is @attribute. Maybe just try accessing it without calling?
- tests - lag random navn til hver create/post for en test (litt som bankid kodeord)
- VAT validation
- maybe rename _GET_PATH to GET_PATH etc for pydantic not messing with it (regex _(?<path>\w*_PATH(_\w*)?))
- organize imports better
- credit note partial?
- auto go from PAyment->SalePayment|PurchasePayment